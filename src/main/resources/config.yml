# Bypass Mode:
# no_count = Players with the admit.bypass permission
#            will not count towards the player limit
#
# override = Players with the admit.bypass permission will
#            join the server, regardless of player limit
bypass-mode: no_count
enabled: true

# Spooky stuff (Do Not Touch)
config-version: 1.0.0